version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm install
    build:
      commands:
        - mkdir dist
        - echo "SSM_PREFIX="$SSM_PREFIX > dist/.env
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'